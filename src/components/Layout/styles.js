import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  main: {
    position: 'relative',
    overflow: 'hidden',
    width: '100vw',
  },
}));

export default useStyles;
